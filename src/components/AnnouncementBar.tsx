const AnnouncementBar = () => {
  return (
    <div className="bg-primary text-primary-foreground text-center py-2.5 px-4">
      <p className="text-xs tracking-[0.2em] uppercase font-light">
        Complimentary reverse pickup on first-order exchanges across India
      </p>
    </div>
  );
};

export default AnnouncementBar;
